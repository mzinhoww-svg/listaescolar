# E2E S03 · Importação INEP (`/admin/importacoes`)

Ambiente: trilha 1 (Supabase local, portas 544xx), `pnpm db:reset`, `.env.local` gerado de `pnpm db:env` (não versionado), `pnpm build && PORT=3001 pnpm start`, agent-browser (viewport 1280×800). Sessão nomeada e única do agent-browser (`s03wave-t1-7f3a`); login por magic link lendo a API do Mailpit (`http://127.0.0.1:54424/api/v1/messages`) como `admin@listacerta.test` e `parent@listacerta.test`.

Nota: `agent-browser upload` congelou a aba ao enviar o formulário com o arquivo; o arquivo foi injetado com `DataTransfer` + `new File([...])` via `eval` e `form.requestSubmit()` (mesmo fluxo do navegador, sem o helper de upload).

| # | Passo | Esperado | Resultado |
|---|-------|----------|-----------|
| 1 | Admin abre `/admin/importacoes` sem lotes (banco recém-resetado) | Estado vazio; cartões "Escolas reais" 0 e "Escolas de demonstração" 0 (valores lidos do banco, não spinner); botão "Escolher arquivo" em português | OK (S03-01-vazio.png, captura real do estado vazio) |
| 2 | Upload de `tests/fixtures/inep-demo.csv` (INEPs 9900100x) com "demonstração" | inseridas 3, atualizadas 0, sem alteração 0, duplicadas 2, rejeitadas 3; selo Demonstração; "Escolas reais" 0 e "Escolas de demonstração" 3; lote na tabela | OK (S03-02-sucesso-demo.png mostra o cartão de resultado "Importação concluída" com selo Demonstração e as contagens 3/0/0/2/3) |
| 3 | Reupload do mesmo arquivo | "Arquivo já importado"; contagens 3/0/0/2/3 iguais; contagem demo continua 3; um só lote | OK (S03-03-ja-importado.png) |
| 4 | CSV sem coluna `TP_DEPENDENCIA` | Erro do arquivo: "Coluna obrigatória ausente: TP_DEPENDENCIA"; lote `failed` (0 linhas) | OK (S03-04-erro-colunas.png) |
| 4b | Mesmo arquivo do passo 2, agora sem marcar "demonstração" | Não reprocessa; erro "Este arquivo já foi importado como demonstração; a marcação enviada é de dados reais." | OK (S03-04b-marcacao-divergente.png) |
| 5 | `GET /admin/importacoes/<lote>/erros.csv` como admin | 200, `text/csv`, `Content-Disposition: attachment; filename="erros-importacao-<uuid>.csv"`, 5 linhas nas linhas do ARQUIVO 5 a 9 (2 duplicadas, 3 rejeitadas), mensagens legíveis | OK |
| 6 | Detalhe do lote | Total 8, inseridas 3, sem alteração 0, duplicadas 2, rejeitadas 3, "Linhas com aviso" 0, lista "Linha 5" a "Linha 9" (linha do arquivo, cabeçalho = 1) | OK (S03-05-detalhe-lote.png, hoje do lote com município movido: "Linhas com aviso" 1, filtro PostgREST exercitado) |
| 7 | `parent` em `/admin/importacoes` | Tela 403; `erros.csv` responde 403 | OK (S03-06-parent-403.png) |
| 8 | Anônimo em página e `erros.csv` | 307 para `/entrar?next=...` | OK |

Comparação com Admin02: barra lateral escura, selo "Admin interno", título, cartões brancos tonais com raio 24, cabeçalho de tabela em caixa alta cinza; sem números inventados.


## Onda de correção da revisão final

| # | Passo | Esperado | Resultado |
|---|-------|----------|-----------|
| 9 | Falha parcial: CSV de 600 linhas (demo) com um gatilho SQL temporário que derruba o INSERT da linha 551 | Cartão "Importação falhou", 500 inseridas, texto de retomada, botões "Tentar novamente" e "Ver detalhes"; contagem demo sobe 500 | OK (S03-07-falha-parcial.png) |
| 10 | Gatilho removido; o campo de arquivo é ZERADO por script (`input.value = ''`, simulando o reset do formulário) e clica em "Tentar novamente" | Reenvia o mesmo arquivo em memória: "Importação concluída" + "Importação retomada", 600 inseridas, 603 demo no total (nenhuma duplicada) | OK (S03-08-retry-ok.png) |
| 11 | CSV sem colunas obrigatórias (lote `failed`); recarregar a lista e abrir o detalhe | Erros do arquivo persistidos em `import_batches.file_errors`, "Erros do arquivo" no detalhe e linha "Arquivo" no `erros.csv` | OK (S03-10-detalhe-erro-arquivo.png; SQL confirmou o jsonb) |
| 12 | Lista de lotes a 1280 px | Sem rolagem horizontal; colunas Arquivo e data, Status, Resultado, Erros | OK (S03-09-lista-lotes.png) |
| 13 | CSV de 4,16 MB | Recusado no cliente: "O arquivo excede 4 MB. Arquivos maiores entram pelo script de importação." | OK (S03-11-excede-4mb.png) |
| 14 | CSV de 3,9 MB (29 270 linhas) | Chega ao servidor (limite do Next em 4 MB) e conclui em cerca de 6 s: 29 270 inseridas | OK (S03-12-upload-3-9mb.png) |
| 15 | `pnpm import:inep tests/fixtures/inep-demo.csv --demo` no banco local; segunda execução; alvo `https://abcdefghijklmnopqrst.supabase.co` | 3/0/0/2/3 e caminho do erros.csv; segunda execução "lote já existia"; alvo desconhecido recusado sem a flag | OK |
| 16 | CSV com uma escola movida para outro município habilitado (via script) e detalhe do lote | "Linhas com aviso" = 1 (predicado PostgREST real) | OK (S03-05-detalhe-lote.png) |

Não capturado: a tela de carregamento (`loading.tsx` com a casca do admin) pisca rápido demais no build local.
