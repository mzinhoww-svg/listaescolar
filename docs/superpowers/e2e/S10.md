# E2E S10 · Revisão humana (admin) e revisão da própria lista (pai)

Nenhuma chamada ao OpenRouter e nenhuma chave real: provedor FALSO da S08 (`FAKE_AI_SCRIPT`, rotas `fake` por dado) e portas de publicação EM MEMÓRIA da S09 (`FAKE_PUBLICATION_FIXTURE`, com `label` da escola), só com `APP_ENV=local`. Dados sintéticos ("(exemplo)", escola fictícia, e-mails `@listacerta.test`). O PDF enviado é um arquivo mínimo sintético: o visualizador do Chrome mostra "Failed to load PDF document" na captura, o que é esperado (o que se prova é a rota e o iframe).

```
pnpm db:start && pnpm db:reset
node scripts/supa.mjs env > .env.local     # só neste worktree; ignorado pelo git
pnpm build
bash scripts/e2e-s10.sh                     # sessões agent-browser t2s10-*; sobe/derruba só o app (3002) por PID/porta
```
Trava de custo: aborta (código 2) se `OPENROUTER_KEY`/`AI_MODEL_*` estiverem no shell; o app sobe com `env -u`. O worker não é necessário. O interruptor `ai_settings.auto_publish_enabled` é ligado por SQL só para este cenário.

Resultado da última execução: **123 verificações, 0 falhas** (`PASS=123 FAIL=0`).

## Cenários (verificação por SQL em `ai_decisions`/`review_versions`/`parent_list_copies`)
| # | Cenário | Verificado |
|---|---|---|
| a | Aceite do PLAN: `school_member` envia lista manuscrita (crítico no seed) com item sem quantidade; admin corrige e publica | Fila com "Alerta crítico no documento", sem dado do remetente; documento pela rota 307 (Location no bucket privado, `no-store`; sem sessão vai ao login; HTML sem URL assinada/`storage_path`); aprovar bloqueado com pendência, com edição não salva e sem a confirmação "Conferi o documento original"; salvar cria v2; "Lista publicada." + selo "Demonstração". `extraction:accepted > publication:human_review > review:edited > review:approved > review:published`; 3 linhas `review` com `actor_id` do admin; v1 preservada (quantidade nula) e v2 com a correção; `edited` previous=v1/new=v2; `approved` aponta v2 com `critical_alerts_acknowledged`; envio `published`; nenhum texto de item em `ai_decisions`; a escola vê "Lista publicada" (D-071), nunca "Publicada automaticamente". Capturas `S10-fila`, `S10-detalhe`, `S10-publicada`. |
| b | Stale entre dois admins (dois contextos) | O 2º a salvar recebe o aviso, mantém o rascunho, nenhuma versão perdida (v1, v2 do admin 1); só após "Recarregar a versão mais recente" vê a versão do outro. Captura `S10-stale`. |
| c | Recusa por motivo de lista fechada + nome hostil | Nome `<img src=x onerror=alert(1)>` volta como texto (0 nós img/script); `review:rejected` com `["illegible_document"]`; aba Recusadas (1); a escola vê o aviso sem código de motivo e sem falar em cópia. |
| d | Resultado inválido | Versão 1 nasce vazia, explicação "recuse ou digite os itens", aprovação bloqueada, recusa. |
| f | Pai: edita a cópia | "Revisar meus itens" no andamento; cópia abre com a extração; "Salvar minha lista" (sem "Enviar para revisão"); link "Montar carrinho" = `/carrinho/novo?lista=<copyId>`; SQL: itens editados, versão 2/3; 0 `review_versions`, 0 `ai_decisions review`, envio segue `human_review`; nome hostil como texto; carrinho mostra o estado honesto; o admin vê os itens da EXTRAÇÃO, nada da cópia; após a recusa do admin o pai vê o aviso "Você ainda pode usar sua cópia" e a cópia permanece. Captura `S10-pai-revisar` (390 px). |
| g | Segurança | `parent`/`school_member` em `/admin/revisao` recebem a página 403; rota do documento negada a eles (o proxy responde 403 antes da rota; o 404 da rota é de teste de unidade); outro pai, `school_member` (envio de escola) e admin em `/enviar-lista/<id>/revisar` = 404; sem sessão = 307 ao login. |
| h | A11y | Snapshot do admin e do pai trazem os rótulos de todos os campos e botões. |
| e | Sem porta de publicação | "Escola não identificada neste ambiente"; aprovar mostra "Publicação indisponível neste ambiente até a integração"; o aviso PERSISTE ao virar `approved` e ao reabrir (somente leitura); "Publicar" desabilitado; envio `approved`, cadeia termina em `review:approved`; aba Aprovadas "Aguardando publicação". Captura `S10-sem-porta`. |

Bugs achados só no navegador e corrigidos nesta rodada: (1) primeira abertura de um envio dava 404 por memoização de `fetch` na renderização (`createAdminClient({ fresh: true })`); (2) "Lista recusada." sumia ao envio virar `rejected`.

## O que fica para a S11 (ADR-004 item 9)
- Publicação REAL em `school_lists`/`list_versions` (aqui só a porta em memória) e o E2E ponta a ponta (lista publicada aparecendo no perfil da escola); `ListPublisher` real aceitando ator admin, `parent_upload` e chave = versão aprovada, publicando o acumulado de envios `approved`; `SchoolLabelReader` real; seletor de escola na revisão para envio sem `school_id`; conciliação de `publish_orphaned`.
- `ListReader` do carrinho lendo `parent_list_copies` com checagem de dono (até lá, `/carrinho/novo?lista=<copyId>` mostra o estado honesto).
- Revisão da escola (`/escola/listas/[id]/revisar`, Escola09) depois do vínculo `school_members` (D-002); "Colar texto" do App15 (S15).
- S19: CSP com `frame-src`/`img-src` para o iframe/imagem do documento.
- Nenhuma variável `FAKE_*` no staging/produção.
