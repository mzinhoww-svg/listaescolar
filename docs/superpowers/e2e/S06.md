# E2E S06 · Reivindicação de escola

Ambiente (trilha 1, tudo local, só dados demonstrativos): `pnpm db:start && pnpm db:reset`; `.env.local` só neste worktree (ignorado pelo git) com `node scripts/supa.mjs env` mais `APP_ENV=local`; `pnpm import:inep tests/fixtures/inep-demo.csv --demo`; `pnpm seed:demo-claims` (cria em 99001002 um pedido `awaiting_verification` por documentos com o PDF fictício e em 99001003 um `rejected` com motivo, de `parent@listacerta.test`; nunca aprova); `pnpm build && PORT=3001 pnpm start`. Navegador: agent-browser, sessões `t1s06-*` (perfil em 390x844; telas do responsável e do admin em 1280x800). Login por link mágico lido do Mailpit. Roteiro executável: `scripts/e2e-s06.sh` (não contém chave; o arquivo enviado é `tests/fixtures/claim-evidence-demo.pdf`, injetado por `DataTransfer`, Ruling S03 T3; o `upload` do agent-browser trava o renderer neste input).

- **Fase 1** (`PHASE=1`, servidor SEM `DEMO_CLAIM_DELIVERY`): **58 asserções, 0 falhas** (`wait_text` agora conta como asserção).
- **Fase 2** (`PHASE=2`, servidor reiniciado com `DEMO_CLAIM_DELIVERY=1 APP_ENV=local`, link/código impressos no log do servidor): **21 asserções, 0 falhas** (banco reposto e semeado antes; a fase 2 só depende do seed; para reiniciar encerre só o servidor que você abriu com `kill "$(lsof -ti tcp:3001 -sTCP:LISTEN)"`, nunca `pkill -f`).

| # | Cenário | Resultado observado |
|---|---|---|
| a | Anônimo em `/escolas/99001001` | Selo Demonstração e bloco estado 1 ("Você trabalha nesta escola?"); `/reivindicar` vai a `/entrar?next=`; INEP inexistente = 404 da marca |
| b | Parent logado, escola demo `registered` | Escola01 adaptada: "Encontrada no cadastro do INEP" (nunca "verificada"), e-mail e WhatsApp "Indisponível: ..." com o motivo sem sender, e-mail da sessão exibido; documentos: criar, aviso de dados de alunos, envio sem arquivo desabilitado com motivo, arquivo por `DataTransfer`, "Enviar para análise" -> `awaiting_verification`, escola `claimed`, linha do tempo com datas reais e passo aberto, sem "[prazo]" |
| c | Perfil depois do envio (390) | Selo "Reivindicada" e bloco estado 3 ("Enviada em [data real]" + "Ver status"; eyebrow "Página da escola", o número do estado fica só em `data-claim-state`); escola com reivindicação recusada (seed) mostra estado 4 com o motivo real |
| d | Parent em `/admin/reivindicacoes` e `/escola` (papel parent) | HTTP 403 nos dois (acesso decidido por `features/auth/access.ts`, não alterado) |
| e | Admin: fila (Admin04) | Abas com contagem do banco ("Pendentes (2)"), escola, reivindicante, e-mail da conta, método, canal, N/500, arquivos, selos "Escola sem admin" e "Demonstração" |
| f | Admin: detalhe | A rota da evidência responde com redirect para o admin (medido: `fetch` com `redirect: manual` = `opaqueredirect`; o destino ser a URL assinada de 60 s é coberto por `tests/claims/evidence-route.test.ts`, pois o headless aborta a navegação ao PDF); aprovar -> claim `approved`, escola `verified`, parent vira `school_member`, vínculo `owner` criado; "Pendentes (1)" |
| g | Perfil público depois da aprovação | "Escola verificada" e bloco estado 2 (texto, sem botão nem promessa de convite) |
| h | Parent em `/escola` (Escola03) | Escola vinculada "Verificada", reivindicações recusada (com motivo) e em análise (com data); sem contagem de listas (sem fonte) |
| i | Pedir mais evidências -> reenvio -> recusa | Parent vê "Precisa de mais evidências" e o motivo; adiciona arquivo e nota e reenvia (`awaiting_verification`); admin recusa com motivo; escola volta a `registered`; perfil mostra estado 4 com o motivo |
| j | Vazamento | HTML e RSC (`fetch` com `RSC: 1`) sem e-mail (`demoN@`), telefone (`3333000N`, `999990003`), `decided_by`, `actor_id`, `storage_path`, `token_hash` nem `code_hash` em: formulário, status, fila, detalhe do admin, painel de token (WhatsApp), página `/confirmar` e tela pós-confirmação por e-mail. No perfil logado só se procuram e-mail e colunas internas (o telefone público do perfil é da S04). Não foi medido: ausência do token/código em claro nas respostas |
| k | Fase 2, WhatsApp (99001003) | Métodos por token ficam disponíveis com o sender de demo; enviar código; código errado recusado com texto fixo; correto confirma o canal; fila mostra "Canal confirmado em"; a equipe recusa |
| l | Fase 2, e-mail (99001003, "reivindicar de novo") | Link impresso no log; o GET da página só mostra o botão e NÃO consome o token (banco confere); o botão confirma; reabrir o link diz "já foi confirmado" |

Screenshots (`docs/superpowers/e2e/screenshots/`): `S06-01-bloco-estado1`, `S06-02-formulario` (Escola01), `S06-03-documentos`, `S06-04-em-analise` (Escola02), `S06-05-bloco-estado3` e `S06-06-bloco-estado4` e `S06-09-verificada-estado2` (App14b), `S06-07-fila-admin` (Admin04), `S06-08-detalhe-admin`, `S06-10-minhas-escolas` (Escola03), `S06-11-mais-evidencias`, `S06-12-token-confirmado`, `S06-13-confirmar-email`, `S06-14-fila-canal-confirmado`. Só dados demonstrativos e contas `@listacerta.test`.

Diferenças em relação às telas de referência (Rulings em `ledger-dados.md`): Escola01 perde o passo "Endereço" e usa três passos (Pedido, Verificação, Análise); Escola02 sem "[prazo]" nem "avisamos por e-mail"; App14b estado 3 aponta para a página de status e o estado 2 não tem botão "Pedir acesso" (convites adiados); Escola03 sem coluna "Listas" (sem fonte) e sem "Falar com suporte" (sem canal); Admin04 ganha as abas "Evidência pedida", a página de detalhe e o botão "Pedir mais evidências".

Achados do E2E (corrigidos no roteiro): `wait_text "Em análise"` casava com o evento antigo da linha do tempo (o roteiro espera pelo estado no banco); `wait_text` não falhava ao esgotar o tempo e dois textos esperados na fase 2 estavam errados (o modo demonstração diz "Modo demonstração: o código/link foi para o log local"); agora falha e conta. O passo "Canal" do responsável passou a mostrar "Canal: aguardando confirmação/confirmado" (medido na captura S06-12).
