# E2E S07 · Envio de lista, leitura e status

Ambiente (trilha 2, tudo local): `pnpm db:start && pnpm db:reset`; `.env.local` só neste worktree com `pnpm db:env` mais `DEMO_PIPELINE=1`, `APP_ENV=local` (o demo só liga com APP_ENV explícito não produtivo) e `WORKER_SHARED_SECRET` (valor em `/tmp`, fora do repositório); `pnpm build && PORT=3002 pnpm start`; worker servido como em `supabase/functions/ocr-worker/README.md` (`DEMO_SLOW_MS=6000`). Como o `pg_cron` não roda no local, o script chama o worker (POST com o segredo) no papel do agendador. Navegador: agent-browser (390x844; escola em 1280x800), sessões separadas. Login por link mágico lido do Mailpit da trilha (seed: `parent@listacerta.test`, `outro@listacerta.test`, `admin@listacerta.test`). Roteiro executável: `scripts/e2e-s07.sh` (gera os PDFs de teste; nenhum contém dado real).

Resultado da última execução: **43 verificações, 0 falhas**.

| # | Cenário | Resultado esperado (e observado) |
|---|---|---|
| a | Anônimo em `/enviar-lista`; anônimo na rota de status | Vai a `/entrar?next=%2Fenviar-lista`; a rota responde 401. OK |
| b | Parent logado abre `/enviar-lista` (App15/App06) | Aviso de revisão, "Tirar foto" (`input capture=environment`), galeria/PDF, série, ano, consentimento. OK |
| c | Envio sem marcar o consentimento | "Marque o consentimento para enviar a lista."; nada em `list_submissions` nem `consents`. OK |
| d | PNG com nome `.pdf` (Content-Type mentiroso) | Recusado pelo servidor ("O conteúdo do arquivo não confere com o tipo informado"); nada gravado. OK |
| e | Arquivo de 0 bytes | Bloqueado no navegador ("O arquivo está vazio"). OK |
| f | PDF rápido | Tela App20 e depois "Lista lida" com o resumo dos itens do resultado e selo "Demonstração"; banco `review_needed`, `is_demo`, consentimento `list_upload` gravado. OK |
| g | Arquivo "lento" | `processing_async` (1 job); tela com "Continuar aguardando", "Ativar notificação do navegador" (grava `notify_channel='browser'`), canal e-mail/WhatsApp (destino inválido recusado pelo Zod; e-mail válido grava `notify_channel/notify_target`); o worker conclui e o painel muda sozinho para "Lista lida" (polling); `review_needed`, um único `ocr_jobs`. OK |
| h | Outro usuário | `GET /api/submissions/<id>/status` = 404 para o alheio, 200 para o dono; a página do envio alheio mostra o 404 da marca. OK |
| i | Arquivo "falha" | "Não foi possível ler este arquivo"; banco `rejected`. OK |
| j | Escola (1280x800, Escola08) | Parent em `/escola/listas/nova`: 403; admin vê "Enviar PDF da lista" e "Para a leitura sair certa"; sem `?escola` válido: 404; envio conclui com `source='school'` e `school_id`. OK |
| k | Sem pipeline (servidor com `DEMO_PIPELINE=0`, sem segredo do worker) | "Leitura automática indisponível no momento"; nenhum item na tela, `processing_async`, sem `is_demo`, `ocr_jobs` vazio. OK |

Screenshots (`docs/superpowers/e2e/screenshots/`): `S07-enviar-lista.png` (App15/App06), `S07-sem-consentimento.png`, `S07-arquivo-invalido.png`, `S07-resultado.png`, `S07-assincrono.png` (App20 com as opções do estado assíncrono), `S07-assincrono-pronto.png`, `S07-erro.png`, `S07-status-alheio-404.png`, `S07-escola-upload.png`, `S07-escola-arquivo.png` (Escola08), `S07-indisponivel.png`.

Diferenças em relação às telas de referência (registradas no ledger): App15 mostra a edição de itens e o nome da escola; na S07 o pai só envia o arquivo (a revisão de itens é da S10). App20 é a tela de "montando carrinhos"; aqui reaproveitamos a linguagem visual para as etapas reais do envio.

Achados do E2E (corrigidos): (1) o Next entrega o campo de arquivo vazio (foto) como `File` de 0 bytes chamado `blob`; a Server Action passou a preferir o arquivo com conteúdo (teste em `tests/submissions/actions.test.ts`); (2) o upload feito antes da hidratação se perde: o script repete até o nome do arquivo aparecer.
