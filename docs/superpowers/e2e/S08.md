# E2E S08 · Pipeline real de extração com provedores falsos

Nenhuma chamada ao OpenRouter e nenhuma chave real: o provedor é o `FakeProvider`, escolhido por dado (`ai_settings.routes.*.provider = 'fake'`) e roteirizado por `FAKE_AI_SCRIPT` (JSON `{cheap, strong, vision}` com passos). O `fake` só existe com `APP_ENV` explícito em {local, development, preview, staging}; produção nunca o habilita (testes em `tests/extraction/pipeline.test.ts` e `factory.test.ts`). Dados das capturas: itens sintéticos "(exemplo)", nenhum dado real.

Ambiente (trilha 2, tudo local, build de produção):
```
pnpm db:start && pnpm db:reset
node scripts/supa.mjs env > .env.local        # só neste worktree; ignorado pelo git
pnpm build
# worker (Deno) com o mesmo script falso: /tmp/s08-worker.env (WORKER_SHARED_SECRET, APP_ENV=local, FAKE_AI_SCRIPT='...')
pnpm exec supabase --workdir .track-workdir functions serve ocr-worker --no-verify-jwt --env-file /tmp/s08-worker.env &
scripts/e2e-s08.sh                            # sessões agent-browser t2s08-* ; o script sobe/derruba o app em 3002 por PID
```
O script sobe `next start -p 3002` com `APP_ENV=local FAKE_AI_SCRIPT=...` por fase e encerra só pelo PID (mais a porta 3002). Segredo do worker em `/tmp/s08-worker-secret` (fora do repositório).

Roteiro fake: barato = confiança 0,5 (abaixo do limiar 0,80); forte = 3 itens, confiança 0,92 (um item "para a sala" gera `possible_collective_item`).

Resultado da última execução: **18 verificações, 0 falhas**.

| # | Cenário | Verificado (SQL local em `ai_decisions`, tela, banco) |
|---|---|---|
| 1 | Parent envia PDF; barato com baixa confiança escala para o forte | Tela "Lista lida", 3 itens, sinalização "possível uso coletivo" e aviso "Alertas são sinalizações para revisão, não parecer jurídico". `ai_decisions` do envio: `escalated:1:low_confidence:fake-cheap > accepted:2:accepted:fake-strong`; `entity_id` = id do envio; nenhuma decisão contém texto do documento; envio `review_needed`, `is_demo = false`. |
| 2 | `update ai_settings set confidence_threshold = 0.99` (sem deploy; cache de 30 s) | Forte também fica abaixo do limiar: `escalated:1:low_confidence > accepted:2:low_confidence`; tela mostra o aviso em destaque (`role="alert"`) "Leitura automática com baixa confiança: revisão obrigatória antes de qualquer publicação". Nunca "publicável automático" (`requiresReview: true`). |
| 3 | Falha fechada: `ai_settings.routes.*.provider = 'openrouter'` sem chave nem `AI_MODEL_*` | Envio segue `processing_async`; zero decisões (nenhuma rede, nenhuma tentativa), zero `ocr_jobs`. (Restaurado por dado depois.) |
| 4 | Barato lento (13 s > orçamento síncrono de 10 s) | Server Action cancela pelo `AbortSignal` (sem decisão), painel "Continuar aguardando"; tick do worker (Deno, script rápido, mesmo pipeline) conclui, painel muda sozinho para "Lista lida"; `ai_decisions` com a mesma cadeia e `entity_id` = id do envio. |
| 5 | Servidor sem `OPENROUTER_KEY`, `AI_MODEL_*` e `FAKE_AI_SCRIPT` | "Leitura automática indisponível no momento"; nenhuma decisão. |

Capturas (`docs/superpowers/e2e/screenshots/`): `S08-resultado.png`, `S08-baixa-confianca.png`, `S08-falha-fechada.png`, `S08-worker.png`, `S08-indisponivel.png`.

Achado do E2E: o seed da 0202 aponta as rotas para `openrouter`; com o banco recém-resetado e só o `FAKE_AI_SCRIPT`, o envio cai no caminho assíncrono (falha fechada, como deve). O script troca as rotas para `fake` por dado antes de começar.

Limitação registrada: a tela de status decide "indisponível" por ambiente (`getExtractionPipeline() !== null`), não pelo banco; no cenário 3 (troca de rota por dado) o painel mostra "aguardando" em vez de "indisponível", e o worker falha fechado a cada tentativa até a configuração voltar.

Smoke real (custo, ação do humano): `scripts/ai-smoke.ts` com `OPENROUTER_KEY` e `AI_MODEL_*` no ambiente e um arquivo de exemplo; não roda em CI nem pelos agentes.
