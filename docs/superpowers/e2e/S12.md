# E2E S12 · Carrinho, opções, clique afiliado

Ambiente: trilha 3. `pnpm db:start && pnpm db:reset`; `.env.local` só neste worktree (não versionado, removido ao final) com `pnpm db:env` (URL `http://127.0.0.1:54621`, chaves locais, `NEXT_PUBLIC_SITE_URL=http://127.0.0.1:3003`) mais `DEMO_RETAILERS=1` (rodada B); `pnpm build && PORT=3003 pnpm start`. Login por magic link lendo o Mailpit da trilha (`http://127.0.0.1:54624/api/v1/search?query=to:<email>`), usuários de seed `parent@listacerta.test` e `admin@listacerta.test`. agent-browser, viewport 390x844, uma sessão nomeada por usuário. Lista de demonstração: `?lista=00000000-0000-4000-8000-00000000d3a0` (5 itens genéricos). Sem `MELI_AFFILIATE_ID`/`AMAZON_ASSOCIATE_TAG` no ambiente: nenhum selo "link afiliado" deve aparecer (verificado). Captura de tela por passo em `docs/superpowers/e2e/screenshots/S12-*.png`. Nenhum token de link foi registrado.

## Rodada A · `DEMO_RETAILERS=1` (loopback, regra fail-closed liga a demonstração)
| # | Passo | Resultado esperado | Resultado |
|---|---|---|---|
| 1 | Anônimo abre `/carrinho/novo?lista=<demo>` | vai a `/entrar?next=...` | OK |
| 2 | Login do parent; volta a `/carrinho/novo?lista=<demo>` (S12-novo) | "Montar carrinho com 5 itens", aviso de demonstração | OK |
| 3 | "Comparar opções" (S12-carrinho-demo) | `/carrinho/<id>`; 3 opções com total (R$ 251,40 em 2 lojas; R$ 259,56 em 1 loja x2), selo "Demonstração" em cada; `Papelaria local` "indisponível / Sem cotação da papelaria local."; "prazo indisponível"; cada linha com origem "demonstração" e data/hora; "Frete: indisponível"; aviso "Preço e estoque podem mudar"; nenhum selo afiliado | OK |
| 4 | Checkout `/carrinho/<id>/checkout?opcao=cheapest` (S12-checkout-demo) | 2 lojas, subtotais, "Falta abrir", botões "Abrir em ..." que são âncoras para `/ir-para/...` (nunca URL da loja no HTML) | OK |
| 5 | `/ir-para/<id>/mercadolivre` (S12-ir-para) | aviso; sem aviso de afiliado (sem ID); botão "Abrir agora" | OK |
| 6 | `curl` com o cookie do parent em `/ir-para/<id>/mercadolivre/go` | 307, `Location: https://lista.mercadolivre.com.br/L%C3%A1pis%20preto%20HB`, `Cache-Control: no-store` | OK |
| 7 | idem em `/kalunga/go?url=https://evil.example` | 307 `Location: https://www.kalunga.com.br/busca/L%C3%A1pis%20preto%20HB` (parâmetro `url` ignorado) | OK |
| 8 | `curl` sem cookie em `/kalunga/go`; varejista `naoexiste` com cookie | 307 `/entrar?next=...`; 404 | OK |
| 9 | SQL `select r.slug, c.affiliate_applied, c.target_url ... from affiliate_clicks` | uma linha por clique, `affiliate_applied=f`, `target_url` igual ao `Location` | OK (kalunga e mercadolivre) |
| 10 | Checkout depois dos cliques | "2 de 2 lojas abertas", barra a 100%, "Aberto" nas duas | OK |
| 11 | "Já comprei nesta loja" e "Já comprei tudo"; recarregar | "Comprado em ... (desfazer)" persiste no navegador (localStorage) | OK |
| 12 | "Abrir agora" na UI (Sis02) | navegador termina em `https://www.kalunga.com.br/busca/L%C3%A1pis%20preto%20HB`; nova linha em `affiliate_clicks` (total 3) | OK |
| 13 | Admin (sessão separada) abre o carrinho do parent e o checkout (S12-404-alheio) | 404 ("Esta página não está na lista"); em `/go` há 404 real | OK |

## Rodada B · sem `DEMO_RETAILERS` (produção simulada)
| # | Passo | Resultado |
|---|---|---|
| 1 | `/carrinho/novo?lista=<demo>` (S12-novo-sem-fonte) | "Listas indisponíveis por aqui"; nenhum dado inventado |
| 2 | `/carrinho/<id>` do carrinho criado na rodada A (S12-carrinho-sem-fonte) | "Nenhuma opção com preço disponível"; as 4 opções "indisponível", sem nenhum valor em R$, sem botão "Escolher esta" |
| 3 | `/carrinho/<id>/checkout` (S12-checkout-sem-fonte) | "Sem lojas com preço disponível" |

## Observações
- Os cliques do E2E ficam em `affiliate_clicks` local e quebram o teste `admin lê cliques` de `tests/db/cart.test.ts` (espera 1); rodar `pnpm db:reset` depois do E2E (feito).
- `notFound()`/`redirect()` nas páginas devolvem HTTP 200 com a tela 404/login (streaming sob o `loading.tsx` raiz); só `go` devolve 404/307 reais.
- Comparação com as telas: layout mobile 390 fiel a App07/App17/App18/Sis02 no que os dados permitem (sem WhatsApp da papelaria e sem prazo/frete: não há fonte).
