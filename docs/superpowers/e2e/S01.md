# E2E S01 · schema base

A S01 não tem UI. Roteiro:
1. `pnpm db:reset && pnpm test:db` (88 testes: enums, RLS por perfil, auditoria, hardening).
2. Staging (projeto ListaEscolar): conferir por SQL de leitura que as tabelas `municipalities`, `profiles`, `audit_log` existem com RLS ativo (`pg_class.relrowsecurity`), que Cuiabá está habilitado e que `get_advisors` (security) não aponta tabela sem RLS.
