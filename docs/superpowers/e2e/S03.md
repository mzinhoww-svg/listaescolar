# E2E S03 · Importação INEP (`/admin/importacoes`)

Ambiente: trilha 1 (Supabase local, portas 544xx), `pnpm db:reset`, `.env.local` gerado de `pnpm db:env` (não versionado), `pnpm build && PORT=3001 pnpm start`, agent-browser (viewport 1280×800). Login por magic link lendo a API do Mailpit (`http://127.0.0.1:54424/api/v1/messages`) como `admin@listacerta.test` e `parent@listacerta.test`.

Nota: `agent-browser upload` congelou a aba ao enviar o formulário com o arquivo; o arquivo foi injetado com `DataTransfer` + `new File([...])` via `eval` e `form.requestSubmit()` (mesmo fluxo do navegador, sem o helper de upload).

| # | Passo | Esperado | Resultado |
|---|-------|----------|-----------|
| 1 | Admin abre `/admin/importacoes` sem lotes | Estado vazio, contagem 0 vinda do banco | OK (S03-01-vazio.png) |
| 2 | Upload de `tests/fixtures/inep-demo.csv` com "demonstração" | inseridas 3, atualizadas 0, sem alteração 0, duplicadas 2, rejeitadas 3; selo Demonstração; contagem de escolas 3; lote na tabela | OK (S03-02-sucesso-demo.png) |
| 3 | Reupload do mesmo arquivo | "Arquivo já importado"; contagem continua 3; um só lote | OK (S03-03-ja-importado.png) |
| 4 | CSV sem coluna `TP_DEPENDENCIA` | Erro do arquivo: "Coluna obrigatória ausente: TP_DEPENDENCIA"; lote `failed` | OK (S03-04-erro-colunas.png) |
| 5 | `GET /admin/importacoes/<lote>/erros.csv` como admin | 200, `text/csv`, `Content-Disposition: attachment; filename="erros-importacao-<uuid>.csv"`, 5 linhas (2 duplicadas, 3 rejeitadas), mensagens legíveis, células neutralizadas | OK |
| 6 | Detalhe do lote | Totais e lista de problemas | OK (S03-05-detalhe-lote.png) |
| 7 | `parent` em `/admin/importacoes` | Tela 403; `erros.csv` responde 403 | OK (S03-06-parent-403.png) |
| 8 | Anônimo em página e `erros.csv` | 307 para `/entrar?next=...` | OK |

Comparação com Admin02: barra lateral escura, selo "Admin interno", título, cartões brancos tonais com raio 24, cabeçalho de tabela em caixa alta cinza; sem números inventados.
