# E2E S13 · Papelarias: credenciamento, catálogo, perfil público e admin

Ambiente: trilha 3. `pnpm db:reset`; `.env.local` só neste worktree (não versionado, removido ao final) com `pnpm db:env` (URL `http://127.0.0.1:54621`, `NEXT_PUBLIC_SITE_URL=http://127.0.0.1:3003`); `pnpm build && PORT=3003 pnpm start`. Login por magic link lendo o Mailpit da trilha (`http://127.0.0.1:54624/api/v1/search?query=to:<email>`); usuários de seed `parent@listacerta.test` e `admin@listacerta.test` mais `outro@listacerta.test` (terceiro) e `escola@listacerta.test` (promovido a `school_member` por SQL local). agent-browser, uma sessão nomeada por usuário (desktop 1280x800; perfil público 390x844). Estado conferido no Postgres local (`docker exec supabase_db_listacerta-t3 psql`). Nenhum token de link registrado. Capturas em `docs/superpowers/e2e/screenshots/S13-*.png`.

Dados do teste: papelaria "Papelaria Boa Vista", CNPJ de teste `11.222.333/0001-81` (formato válido, sem relação com empresa real). Planilha: 5 linhas (2 válidas, 1 fórmula `=HYPERLINK(...)`, 1 preço `abc`, 1 estoque "nao").

| # | Passo | Resultado esperado | Resultado |
|---|---|---|---|
| 1 | Parent abre `/cadastrar-papelaria` (S13-cadastro-passo1) | Stepper (Dados da loja, Atendimento, Confirmação) + painel escuro "Como você recebe leads" só com o texto do design | OK |
| 2 | CNPJ `11.111.111/1111-11` e "Continuar" (S13-cadastro-cnpj-invalido) | Fica no passo 1, "CNPJ inválido." | OK |
| 3 | CNPJ válido, passo 2 (S13-cadastro-passo2) | WhatsApp, horário, bairros (chips), raio, pagamentos, entrega/retirada; link de teste `https://wa.me/5565999991234?text=...` (só abre a conversa, nada enviado) | OK |
| 4 | Passo 3: enviar sem LGPD, depois com | Sem aceite: erro no campo; com aceite: `signup→accreditation→under_review` (2 eventos `owner`), papelaria `under_review`, bairros gravados (S13-credenciamento-em-analise) | OK |
| 5 | Erro no envio devolve os valores digitados | Campos, município, bairros e pagamentos preservados (defeito do `<select>` achado neste passo e corrigido com `key`) | OK |
| 6 | Parent tenta cadastrar CNPJ já usado (S13-cadastro-cnpj-duplicado) | "Já existe uma papelaria com este CNPJ." no campo; nada criado | OK |
| 7 | Admin de seed em `/cadastrar-papelaria` (S13-admin-nao-cadastra); `school_member` idem (S13-school-member-bloqueado) | Mensagem "Só uma conta de responsável ... pode cadastrar uma papelaria." | OK |
| 8 | Admin abre `/admin/papelarias` (S13-admin-fila) e o detalhe (S13-admin-detalhe) | Contadores, abas, busca; linha com CNPJ formatado, Recusar/Aprovar; detalhe com dados de cadastro e ações | OK |
| 9 | Admin aprova (S13-admin-aprovada) | `approved`; o perfil do dono passa de `parent` a `stationery_member` | OK |
| 10 | Dono em `/cadastrar-papelaria` e `/papelaria` (S13-painel-aprovada) | Primeira: "Você já tem uma papelaria aprovada" com link ao painel; painel acessível; catálogo liberado (S13-catalogo-vazio-aprovada) | OK |
| 11 | Dono publica (S13-painel-ativa) | `approved→active`; `/papelarias/<slug>` abre (S13-perfil-sem-itens: sem preços, "Pergunte pelo WhatsApp") | OK |
| 12 | Catálogo manual: "Cola branca 90g", R$ 4,50, "Tenho" | Item salvo; tabela com preço, "Informado pela papelaria" e data do preço | OK |
| 13 | Importar planilha (S13-catalogo-importado) | "3 de 5 linhas importadas. 2 com erro."; arquivo baixado com cabeçalho `linha,campo,erro,valor` e a célula da fórmula neutralizada (`'=HYPERLINK(...)`) | OK |
| 14 | Perfil público em 390x844 (S13-perfil-publico) | Nome, bairro, retirada/entrega, pagamentos, horário, bairros, itens com origem e data; sem avaliações, selos, prazos; botão "Pedir lista pelo WhatsApp"; `robots: noindex, nofollow`; HTML sem CNPJ, razão social nem e-mail | OK |
| 15 | Provedor de cotação local no banco (ativa) | 2 cotações (cola 450, caderno 1890); "borracha" (estoque "nao") omitida | OK |
| 16 | Admin suspende com motivo (S13-admin-suspensa) | `suspended`; perfil público vira "Esta página não está na lista" (S13-perfil-suspensa-404); catálogo do dono bloqueado por status; cotação local devolve `[]` | OK |
| 17 | Terceiro (`parent` sem papelaria) em `/papelaria`, `/papelaria/catalogo`, `/admin/papelarias`, `/admin/papelarias/<id>` (S13-terceiro-403) | Tela 403 (nenhuma rota de dono recebe id por input; tudo vem da sessão) | OK |
| 18 | Segunda papelaria (CNPJ `11.444.777/0001-61`): admin recusa com motivo; dono vê o motivo (S13-credenciamento-recusada) e reenvia | `rejected` com motivo; "Reenviar para análise" leva a `under_review` (eventos `rejected→accreditation→under_review`) | OK |

## Observações
- Comparado às telas: a fila do admin e o catálogo seguem Admin09/Pap04 (menu lateral Tinta, cartões, tabela). O cadastro (Pap01) fica sem o menu lateral porque quem cadastra ainda é `parent` (o menu só existe depois da aprovação). O perfil público (Pap08) mostra só o que o produto sabe: sem nota, avaliações, "Parceira da escola", prazo de entrega nem distância.
- Um `notFound` responde com a tela "Esta página não está na lista" e status HTTP 200, porque o `app/loading.tsx` global inicia o streaming antes da checagem (comportamento existente desde S00; o HTML leva `noindex`).
- Nomes de bairro voltam em minúsculas do banco (chave normalizada do repositório); o perfil público os exibe capitalizados por CSS.

## Reexecução · rodada de correção 2 (Task 2: repositório e ator)
Mesmo ambiente (trilha 3, `pnpm db:reset`, `pnpm build && PORT=3003 pnpm start`, Mailpit da trilha), sessões agent-browser com nome único (`s13r2-8f3a-*`), servidor encerrado pelo PID. Capturas `S13r2-*.png`. Mudanças cobertas: cadastro atômico com aceite obrigatório, ator de sessão, CNPJ alfanumérico, bairros com texto de exibição, candidatos por função SQL, mapeamento de erros por causa.

| # | Passo | Resultado | Resultado |
|---|---|---|---|
| R1 | Parent, CNPJ alfanumérico com dígito errado `12.ab.c3450/1de-34` (S13r2-cnpj-invalido); depois válido `12.abc.345/01de-35` | Passo 1 recusa; o válido (caixa baixa, com máscara) avança; banco guarda `12ABC34501DE35` | OK |
| R2 | Passo 2 com WhatsApp `0 65 99999-1234` (zero de tronco) e bairros "São José", "Jardim Tropical" (S13r2-cadastro-passo2) | Grava `+5565999991234`; áreas `sao jose`/`jardim tropical` com `display_name` "São José"/"Jardim Tropical" | OK |
| R3 | Passo 3 sem aceite, depois com aceite (S13r2-em-analise) | Sem aceite: "É preciso aceitar o tratamento de dados."; com aceite: `under_review`, `lgpd_text_version = lgpd-papelaria-v1` (constante do servidor), `lgpd_accepted_at` do banco, 2 eventos `owner` | OK |
| R4 | Outro responsável tenta o mesmo CNPJ (S13r2-cnpj-duplicado) | "Já existe uma papelaria cadastrada com este CNPJ." no campo e na página; nada criado (1 papelaria no banco) | OK |
| R5 | Admin: fila (S13r2-admin-fila) com CNPJ formatado `12.ABC.345/01DE-35`; detalhe e Aprovar (S13r2-admin-detalhe) | `approved`; perfil do dono passa a `stationery_member` | OK |
| R6 | Dono: painel, Publicar, bairros (S13r2-painel-aprovada, S13r2-painel-ativa, S13r2-bairros) | `active` (ator da sessão) | OK |
| R7 | Catálogo: item manual R$ 4,50 e planilha (`;`, 4 linhas: 2 válidas, 1 fórmula, 1 preço `abc`) (S13r2-catalogo-importado) | "2 de 4 linhas importadas. 2 com erro."; relatório neutralizado (`'=HYPERLINK(...)`); 3 itens no banco | OK |
| R8 | Perfil público em 390x844 (S13r2-perfil-publico) | Bairros com o texto original ("Jardim Tropical", "São José"); itens com origem e data; `noindex, nofollow`; HTML sem CNPJ, razão social nem e-mail | OK |
| R9 | Candidatos da cotação local (`stationery_local_candidates`, pelo banco) | 2 candidatos (item em falta omitido); 0 depois da suspensão | OK |
| R10 | Admin suspende com motivo (S13r2-admin-suspensa); perfil público (S13r2-perfil-suspensa) | `suspended`; "Esta página não está na lista" | OK |
| R11 | Terceiro em `/papelaria` e `/admin/papelarias` (S13r2-terceiro-403); admin em `/cadastrar-papelaria` (S13r2-admin-nao-cadastra) | 403; mensagem "Só uma conta de responsável ..." | OK |
| R12 | Segunda papelaria (CNPJ `11.444.777/0001-61`): admin recusa com motivo, dono vê o motivo (S13r2-credenciamento-recusada) e reenvia | Eventos `rejected -> accreditation -> under_review` | OK |
